version: 2
updates:
  - package-ecosystem: "maven"
    directory: "/"                 # path to pom.xml
    schedule:
      interval: "daily"
    open-pull-requests-limit: 5
    allow:
      - dependency-type: "direct"
    ignore:
      - dependency-name: "org.springframework.boot:spring-boot-starter"  # Optional

